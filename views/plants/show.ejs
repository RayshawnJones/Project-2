<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= plant.name %></title>
</head>
<body>
  <h1><%= plant.name %></h1>
  <% if(plant.isReadyToEat){ %>
    <p>This plant is ready to pick up</p>
  <% } else { %>
    <p>This plant is not ready for order</p>
  <% } %>
  <form action="/plants/<%=fruit._id%>?_method=DELETE" method="POST">
    <button type="submit">
      Delete <%= plant.name %>
    </button>
  </form>
  <a href="/plants/<%= plant._id %>/edit">Edit <%= plant.name %></a>
  <a href="/plants">Back to Plants</a>
</body>
</html>